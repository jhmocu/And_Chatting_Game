<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.android.chatting_game">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppTheme.NoActionBar" />
        >
        <activity
            android:name=".ProfileInfoActivity"
            android:label="@string/title_activity_profile_info"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Profile_My_info"
            android:label="@string/title_activity_profile__my_info"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Long_Click_name_Update"
            android:label="@string/title_activity_long__click_name__update"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".StatusEditActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".AddFriendsActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ChatRoomActivity" />
        <activity android:name=".NoticeActivity" />
        <activity android:name=".FontChangeActivity" />
        <activity android:name=".BackgroundColorChangeActivity" />
        <activity android:name=".VersionActivity" />


        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />

        <!-- <activity android:name=".VersionActivity"></activity> -->
        <activity android:name=".EditChatListActivity" />
        <activity android:name=".StartAppActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AuthActivity"
            android:theme="@style/AppTheme.NoActionBar"/>
        <activity android:name=".InsertProfileActivity"
            android:theme="@style/AppTheme.NoActionBar"/>

        <service
            android:name=".MyService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".MultiAddChatActivity"></activity>

        <activity
            android:name=".DeleteChatRoomActivity"
            android:label="@string/title_activity_delete_chat_room"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>